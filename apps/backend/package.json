{
  "name": "@teloche/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "wrangler d1 migrations apply DB --local && wrangler dev",
    "dev:api": "wrangler d1 migrations apply DB --local && wrangler dev",
    "dev:xtream-docs": "node --watch src/xtream-docs.ts",
    "deploy:cloudflare": "alchemy deploy",
    "destroy:cloudflare": "alchemy destroy",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "start": "wrangler d1 migrations apply DB --local && wrangler dev",
    "start:api": "wrangler d1 migrations apply DB --local && wrangler dev",
    "start:xtream-docs": "node src/xtream-docs.ts",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.98",
    "alchemy": "2.0.0-beta.63",
    "drizzle-orm": "0.45.2",
    "effect": "4.0.0-beta.98",
    "kysely": "0.29.4",
    "kysely-d1": "0.4.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.6",
    "@cloudflare/workers-types": "5.20260717.1",
    "@effect/vitest": "4.0.0-beta.98",
    "@types/node": "24.13.3",
    "drizzle-kit": "0.31.10",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.112.0"
  }
}
