{
  "name": "teloche",
  "version": "0.1.0",
  "private": true,
  "packageManager": "nub@0.4.11",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "cd apps/backend && nub run dev",
    "dev:backend-api": "cd apps/backend && nub run dev",
    "dev:xtream-docs": "cd apps/backend && nub run dev:xtream-docs",
    "dev:tv": "cd apps/tv && nub run start",
    "android:tv": "cd apps/tv && nub run android",
    "db:generate": "cd apps/backend && nub run db:generate",
    "db:migrate:local": "cd apps/backend && nub run db:migrate:local",
    "prebuild:tv": "cd apps/tv && nub run prebuild:tv",
    "test": "cd apps/backend && nub run test",
    "typecheck:backend": "cd apps/backend && nub run typecheck",
    "versions": "node --version && nub --version"
  },
  "devEngines": {
    "packageManager": {
      "name": "nub",
      "version": "0.4.11",
      "onFail": "warn"
    }
  }
}
